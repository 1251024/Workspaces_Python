# -*- coding:utf-8 -*-

from pymongo import MongoClient

client = MongoClient('localhost', 27017)
db = client.test
score = db.score

res01 = score.delete_one({'name':'차은우'})
print(res01.deleted_count)
